{
 
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd server && npm install && cd ../todolist && npm install",
    "build": "cd todolist && npm run build",
    "start": "npm run install && npm run start-server & npm run start-client",
    "start-server": "cd server && node index.js",
    "start-client": "cd todolist && npm run dev"
  },
  
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.1",
    "nodemon": "^3.0.1"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.11"
  }
  
  
}
